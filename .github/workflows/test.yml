name: Testing

on:
  pull_request:
    branches:
      - master
      - main
      - develop
jobs:
  limit_pr:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: php -r "print_r($_SERVER); exit(1);"
